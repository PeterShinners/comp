/// Minimal geometric point definitions (mostly for quick testing on cli)

!shape point ~(x~num y~num)

!pure origin ~nil {x=0 y=0}
!pure transpose ~point {x=pt.y y=pt.x}
!pure scale ~point {
    :param s ~num 
    x=$.x * s 
    y=$.y * s
}
