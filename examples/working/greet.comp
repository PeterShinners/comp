!import /test = comp "test"
!import /str = stdlib "str"
!import /num = stdlib "num"
!import /loop = comp "loop"



!func |main ~{} = {
    @stream = [|infinite]
    zero = [{} |:@stream]
    one = [{} |:@stream]
    two = [{} |:@stream]

    [#pi~num|absolute|print]
}
