; Example demonstrating !main entry point with module configuration

; Configure module metadata
$mod.name = "greeter"
$mod.version = "1.0.0"

; Configure greeting message
$mod.config = {
    greeting = "Hello, World!"
    language = "en"
}

; Main entry point
!main = {
    ; Simply return a greeting message
    "Hello from the greeter module!"
}
