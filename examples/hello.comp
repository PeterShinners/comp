mod.doc = "Simple hello world example"

import os stdlib "core/os"


main (
    username = ["USERNAME" "USER" "LOGNAME"]
        -> find getenv
        -> default "World"
    
    print "Hello, {username}!"
)


; or, more simply?


main_b (
    ["USERNAME" "USER" "LOGNAME"]
    -> each getenv
    -> keep exists
    -> first "World"
    -> print "Hello, {}!"
)
