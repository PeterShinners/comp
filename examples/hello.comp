doc.mod: "Simple hello world example using !main entry point"

!import os stdlib "core/os"

!main [
	a{"USERNAME" "USER" "LOGNAME"}
	-> map([.in -> getenv() ?? #skip])
	-> first("World") $ "Hello, ${}!"
	-> print()
]
